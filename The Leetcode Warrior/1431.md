# easy hai 
```java
 int max = 0;
            for (int element : candies) {
                if (element > max) {
                    max = element;
                }
            }

            for (int elem : candies) {
                ans.add(elem + extraCandies >= max);
            }
```